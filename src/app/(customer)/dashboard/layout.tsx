// Dashboard inherits the shared (customer) layout which provides
// the topbar, bottom nav, and hamburger menu.
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
